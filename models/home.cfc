component{
	
	// Use functions to model and return stuctured data
	// Use functions to interact with a database such as Postgres, MySQL, MSSQL, or SQLite
	// CFML functions offer options such as access, argument types, and local scope. Below is a simple example.
	
	function main(){
		var data = {
			'title' = 'Welcome to FastCFML'
		}
		return data;
	}
	
	
}